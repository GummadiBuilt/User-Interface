import { BlurFormatDirective } from './blur-format.directive';

describe('BlurFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new BlurFormatDirective();
    expect(directive).toBeTruthy();
  });
});
